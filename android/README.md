# Android 客户端

用 Android Studio 打开 `android/` 后运行 `app`。

已实现：

- 登录/注册到自建中继服务端。
- 注册 Android 设备。
- 前台服务常驻通知，保持 WebSocket 在线。
- 接收远端剪贴板文本并写入 Android 剪贴板。
- 在系统允许的情况下轮询本机剪贴板并同步。
- 系统分享入口，用于后续可靠发送文件/图片。

注意：Android 10+ 对后台读取剪贴板有限制，普通应用不能做到完全静默、无限制读取后台剪贴板。
